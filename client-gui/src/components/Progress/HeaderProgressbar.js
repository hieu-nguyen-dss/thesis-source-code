import * as React from 'react'
import PropTypes from 'prop-types'
import {
  LinearProgress,
  Box
} from '@mui/material'

const LinearProgressBar = props => {
  return (
    <Box sx={{ width: '100%' }}>

    </Box>
  )
}
