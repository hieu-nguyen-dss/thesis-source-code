import { Box } from '@mui/material'

import Language from './Language'

const Setting = props => {
  return (
    <Box sx={{ mt: 2, borderRadius: 3 }} >
      <Language />
    </Box>
  )
}

export default Setting
